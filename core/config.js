var app={};

app.root='/home/bmiller/git/zero-data';

app.ws={
    host:'localhost',
    port:8008
}

app.http={
    host:'localhost',
    port:8080,
    rootDir:'/gui/dataParser'
}

module.exports=app;